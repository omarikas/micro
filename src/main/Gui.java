package main;

public class Gui {
	
	//i want to choose exectime for eALU ops (FP adds, subs, multiply, 
	//divide), (integer ADDI or SUBI needed for loops), loads and stores
	//and branches (BNEZ is enough)
	//select size of each statin (add ,mul,store)
	//preload a register of 11 regs with a value

}
